---
- name: Demonstrate  ansible variables
  hosts: localhost
  vars:
   color: red
  tasks:
  - name: print the value of variables defined above
    debug:
      msg: "The value is :{{ color }}" #This Works
      #msg: {{ color  }} is bright # this si doesnt Works
      #msg: this value is : {{ coclor  }} #This is doesnt works
      #msg: "The value is: {{ color  }}" ==> this works
 #Note the msg without double quote works for variable if there is text before and after the variable . Else wont!!     
...